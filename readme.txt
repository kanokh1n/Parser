Спарсить данные не получится, потому что запрос по ссылке возвращает не ту html страницу или сайт блокирует
также не стал менять код под html файл, чтобы ничего не сломать, но сама суть заключается в том,
что я беру ссылку на сайт и количество страниц и формирую из них ссылку с которой беру html страницу и собираю данные,
используя domCrawler. Далее данные идут в бд(сам процесс парсинга и сохранения в базу данных есть, но из-за
проблем с доступом по ссылкам данные не считываются и не сохраняются).
Также не берется не парсится seller и reviewsCount, потому что я не нашел продавца html, а комментарии спарсить не смог
потому что не понял какой параметр использовать.
И соответсвенно нет консольной команды, потому что сам парсер фактически не работает
